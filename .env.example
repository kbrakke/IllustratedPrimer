# OpenAI API Configuration
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Organization ID for project-scoped keys
# OPENAI_ORG_ID=org-your-org-id

# Optional: Model override (defaults to gpt-5)
# OPENAI_MODEL=gpt-5-mini

# Optional: Max output tokens (defaults to 4096)
# Reasoning models use tokens for both reasoning and output, so this should
# be large enough to accommodate both. Increase for longer story generation.
# OPENAI_MAX_TOKENS=4096

# PostgreSQL Database URL
DATABASE_URL=postgres://primer:primer@localhost:5432/primer?sslmode=disable

# For running AI tests
# AI_TESTS_ENABLED=true
